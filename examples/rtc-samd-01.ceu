#include "rtc-samd.ceu"

emit RTC_DATE_SET(18,6,1);
emit RTC_TIME_SET(0,0,0);
emit RTC_ALARM_TIME_SET(_,_,5);
emit RTC_ALARM_MATCH_SET({RTC_MATCH_SS});

_pinMode(13, _OUTPUT);
every RTC_DONE do
    _digitalWrite(13, not _digitalRead(13));
end
